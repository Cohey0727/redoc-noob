description: 400 Bad Request
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: string
          example: error
        errors:
          type: array
          items:
            type: string
            example: xxが有効ではありません
        message:
          type: string
          example: xxが有効ではありません
