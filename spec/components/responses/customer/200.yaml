description: 200 response
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          description: "個人顧客データまたは、法人顧客データ"
          oneOf:
            - $ref: "../../schemas/customers/individual_customer.yaml"
            - $ref: "../../schemas/customers/corporation_customer.yaml"
