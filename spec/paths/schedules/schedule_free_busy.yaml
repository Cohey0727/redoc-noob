post:
  security:
    - APIキー: []
      OAuth2(Internal): [schedules:read]
  tags:
    - Schedules
  summary: Schedule Free Budy
  description: Returns free/busy information for a set of schedules
  requestBody:
    description: schedule free/busy
    content:
      application/json:
        schema:
          required:
            - data
          properties:
            data:
              description: "予定データ"
              $ref: "../../components/schemas/schedules/schedule.yaml"
  responses:
    "200":
      description: 200 response
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                description: "予定データ"
                $ref: "../../components/schemas/schedules/schedule.yaml"
