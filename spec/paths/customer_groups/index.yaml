post:
  security:
    - APIキー: []
      OAuth2(Internal): [customer_groups:create]
  tags:
    - CusomterGroups
  summary: Create CustomerGroup
  description: Create a new customer_group or update
  requestBody:
    description: customer_group create or update
    content:
      application/json:
        schema:
          required:
            - data
          properties:
            data:
              description: "世帯データ"
              $ref: "../../components/schemas/customer_groups/customer_group.yaml"
  responses:
    "200":
      description: 200 response
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                properties:
                  id:
                    type: number
                    example: 12345678
                    description: "割り当て先の世帯ID"
    "400":
      $ref: "../../components/schemas/common/400.yaml"
    "401":
      $ref: "../../components/schemas/common/401.yaml"
    "403":
      $ref: "../../components/schemas/common/403.yaml"
    "503":
      $ref: "../../components/schemas/common/503.yaml"
